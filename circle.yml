machine:
  node:
    version: 8.0.0

dependencies:
  override:
    - cd ./client && yarn && yarn build
    - cd ./server && yarn && npm run build
  cache_directories:
    - "node_modules"

deployment:
 master:
  branch: master
  commands:
   - echo 'deploying'
   - ssh -v root@dalet.tk "cd ~/www/deploy_thesis; ./circle_deployer.sh"